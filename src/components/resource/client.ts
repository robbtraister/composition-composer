'use strict'

export const useResource = () => {
  throw new Error('`useResource` hook is unavailable in the browser')
}

export const Resource = () => {
  throw new Error('`Resource` component is unavailable in the browser')
}

export default Resource
